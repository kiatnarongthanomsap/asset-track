# 🔗 Supabase Integration Guide

## ✅ การเชื่อมต่อ Supabase สำเร็จแล้ว!

แอปพลิเคชัน AssetTrack ได้รับการอัพเดตให้เชื่อมต่อกับ Supabase แทน PHP API แล้ว

## 📋 ข้อมูลการเชื่อมต่อ

- **Supabase URL:** `https://vyeajefodhcwaanuexzg.supabase.co`
- **API Key:** `sb_publishable_mub-bnzYF8bBKnq2b2WXzw_PXKFmslc`

## 📦 ไฟล์ที่สร้าง/แก้ไข

### 1. ไฟล์ Configuration
- **`src/config/supabase.js`** - ตั้งค่า Supabase client

### 2. ไฟล์ Service Layer
- **`src/services/supabaseService.js`** - ฟังก์ชันสำหรับเชื่อมต่อกับ Supabase
  - `login()` - เข้าสู่ระบบ
  - `fetchCategories()` - ดึงหมวดหมู่
  - `fetchAssets()` - ดึงข้อมูลครุภัณฑ์
  - `saveAsset()` - บันทึก/อัพเดตครุภัณฑ์
  - `deleteAsset()` - ลบครุภัณฑ์
  - `updateAssetStatus()` - อัพเดตสถานะ
  - `fetchAuditLogs()` - ดึง audit logs
  - `createAuditLog()` - สร้าง audit log
  - `bulkImportAssets()` - นำเข้าข้อมูลจำนวนมาก

### 3. ไฟล์ที่อัพเดต
- **`src/App.jsx`** - ใช้ Supabase service แทน PHP API
- **`src/components/LoginPage.jsx`** - ใช้ Supabase login
- **`src/components/ExcelImportModal.jsx`** - ใช้ Supabase bulk import

### 4. Dependencies
- **`@supabase/supabase-js`** - ติดตั้งแล้ว

## 🚀 วิธีใช้งาน

### 1. ตรวจสอบการติดตั้ง
```bash
npm list @supabase/supabase-js
```

### 2. รันแอปพลิเคชัน
```bash
npm run dev
```

### 3. เข้าสู่ระบบ
- **Username:** admin
- **Password:** 123456

## 🔧 การตั้งค่า Supabase Database

### 1. สร้าง Schema
รันไฟล์ `supabase_setup.sql` ใน Supabase SQL Editor

### 2. Import ข้อมูล
รันไฟล์ `insert_statements_supabase.sql` เพื่อเพิ่มข้อมูลหมวดหมู่และครุภัณฑ์

### 3. ตรวจสอบการเชื่อมต่อ
```sql
-- ตรวจสอบจำนวนข้อมูล
SELECT COUNT(*) FROM categories;
SELECT COUNT(*) FROM assets;
SELECT COUNT(*) FROM users;
```

## 📊 ฟีเจอร์ที่รองรับ

✅ **Authentication** - เข้าสู่ระบบ
✅ **Categories** - จัดการหมวดหมู่
✅ **Assets** - CRUD ครุภัณฑ์
✅ **Audit Logs** - บันทึกการทำงาน
✅ **Excel Import** - นำเข้าข้อมูลจำนวนมาก
✅ **Status Updates** - อัพเดตสถานะครุภัณฑ์

## 🔒 Security

- ใช้ **Row Level Security (RLS)** ได้ (ถ้าต้องการ)
- API Key เป็น **anon key** (public) - ควรตั้งค่า RLS ใน Supabase
- สำหรับ production ควรใช้ **service role key** สำหรับ operations ที่ต้องการสิทธิ์สูง

## 🐛 Troubleshooting

### ปัญหา: "Failed to fetch"
**วิธีแก้:**
1. ตรวจสอบ Supabase URL และ API Key ใน `src/config/supabase.js`
2. ตรวจสอบว่า Supabase project ยังใช้งานได้
3. ตรวจสอบ Network tab ใน Browser DevTools

### ปัญหา: "relation does not exist"
**วิธีแก้:**
1. รัน `supabase_setup.sql` ใน Supabase SQL Editor
2. ตรวจสอบชื่อตารางให้ตรงกับ schema

### ปัญหา: "duplicate key value violates unique constraint"
**วิธีแก้:**
- ระบบจะข้ามข้อมูลที่ซ้ำอัตโนมัติ (ใช้ upsert)
- ตรวจสอบรหัสครุภัณฑ์ให้ไม่ซ้ำ

## 📝 หมายเหตุ

- ระบบยังรองรับ mock data สำหรับ development
- ถ้า Supabase ไม่สามารถเชื่อมต่อได้ ระบบจะใช้ mock data
- ควรตั้งค่า RLS policies ใน Supabase สำหรับ production

## 🔄 Migration จาก PHP API

หากต้องการย้อนกลับไปใช้ PHP API:
1. เปลี่ยน `API_URL` ใน `src/App.jsx` กลับเป็น `/api-remote/api.php`
2. เปลี่ยน `LoginPage.jsx` ให้ใช้ fetch แทน `supabaseService.login()`
3. เปลี่ยน `ExcelImportModal.jsx` ให้ใช้ fetch แทน `supabaseService.bulkImportAssets()`

---

**อัพเดทล่าสุด:** 24 ธันวาคม 2568

