# 📸 คู่มือการจัดการรูปภาพ - Supabase Storage

## 🎯 ภาพรวม

แอปพลิเคชัน AssetTrack รองรับการเก็บรูปภาพครุภัณฑ์ผ่าน **Supabase Storage** ซึ่งเป็นระบบจัดเก็บไฟล์ที่ปลอดภัยและมีประสิทธิภาพ

## 📋 วิธีการตั้งค่า

### 1. สร้าง Storage Bucket ใน Supabase

1. เปิด **Supabase Dashboard**
2. ไปที่ **Storage** ในเมนูด้านซ้าย
3. คลิก **New bucket**
4. ตั้งชื่อ bucket: `asset-images`
5. เลือก **Public bucket** (ถ้าต้องการให้เข้าถึงได้โดยไม่ต้อง login)
   - หรือ **Private bucket** (ถ้าต้องการความปลอดภัยสูง)
6. คลิก **Create bucket**

### 2. ตั้งค่า Storage Policies (ถ้าใช้ Private bucket)

รันไฟล์ `supabase_storage_setup.sql` ใน SQL Editor เพื่อตั้งค่า policies

หรือตั้งค่าผ่าน Dashboard:
- ไปที่ **Storage** → **Policies**
- สร้าง policies ตามความต้องการ

## 🔧 ฟีเจอร์ที่รองรับ

### ✅ อัพโหลดรูปภาพ
- รองรับไฟล์: JPG, PNG, WEBP, GIF
- ขนาดสูงสุด: 10MB
- อัพโหลดอัตโนมัติเมื่อเลือกไฟล์
- แสดง progress indicator

### ✅ แสดงรูปภาพ
- แสดงรูปภาพใน Grid View และ Table View
- รองรับรูปภาพจาก URL (Unsplash, external links)
- รองรับรูปภาพจาก Supabase Storage

### ✅ ลบรูปภาพ
- ลบรูปภาพจาก Storage เมื่อลบครุภัณฑ์ (ถ้าต้องการ)

## 📝 วิธีใช้งาน

### อัพโหลดรูปภาพใน Edit Asset Modal

1. เปิดหน้า **ทะเบียนทรัพย์สิน**
2. คลิก **เพิ่มทรัพย์สิน** หรือ **แก้ไข** ครุภัณฑ์
3. ในส่วน **รูปภาพ**:
   - คลิกที่พื้นที่รูปภาพ
   - เลือกไฟล์รูปภาพจากเครื่อง
   - รอให้อัพโหลดเสร็จ
   - รูปภาพจะแสดงทันที

### ใช้ URL รูปภาพ (External)

1. ใน Edit Asset Modal
2. กรอก URL ของรูปภาพในช่อง **Image URL** (ถ้ามี)
3. หรือใช้ URL จาก Unsplash, Imgur, หรือบริการอื่นๆ

## 🗂️ โครงสร้างไฟล์

```
asset-images/
├── A004-09-04-2557-1234567890.jpg
├── A010-09-04-2557-1234567891.png
└── ...
```

**รูปแบบชื่อไฟล์:** `{asset-code}-{timestamp}.{extension}`

## 🔒 Security & Best Practices

### 1. Public vs Private Bucket

**Public Bucket:**
- ✅ เข้าถึงได้ง่าย ไม่ต้อง authentication
- ✅ เหมาะสำหรับรูปภาพทั่วไป
- ⚠️ ใครก็เข้าถึงได้

**Private Bucket:**
- ✅ ปลอดภัยกว่า ต้อง authentication
- ✅ ควบคุมการเข้าถึงได้
- ⚠️ ต้องตั้งค่า policies

### 2. File Size Limits

- **แนะนำ:** ไม่เกิน 5MB ต่อไฟล์
- **สูงสุด:** 10MB (ตั้งค่าใน code)
- **รูปแบบ:** JPG, PNG, WEBP, GIF

### 3. Image Optimization

แนะนำให้:
- ใช้ JPG สำหรับรูปภาพถ่าย (ขนาดเล็กกว่า)
- ใช้ PNG สำหรับรูปภาพที่มี transparency
- Resize รูปภาพก่อนอัพโหลด (แนะนำ 800x600px)

## 🛠️ Troubleshooting

### ปัญหา: "Storage bucket not found"

**วิธีแก้:**
1. ตรวจสอบว่า bucket `asset-images` ถูกสร้างแล้ว
2. ตรวจสอบชื่อ bucket ให้ตรงกัน
3. ตรวจสอบ permissions

### ปัญหา: "Upload failed"

**วิธีแก้:**
1. ตรวจสอบขนาดไฟล์ (ต้องไม่เกิน 10MB)
2. ตรวจสอบประเภทไฟล์ (ต้องเป็นรูปภาพ)
3. ตรวจสอบ Storage policies
4. ตรวจสอบ Console สำหรับ error messages

### ปัญหา: รูปภาพไม่แสดง

**วิธีแก้:**
1. ตรวจสอบ URL ของรูปภาพ
2. ตรวจสอบว่า bucket เป็น Public หรือมี policy ที่ถูกต้อง
3. ตรวจสอบ CORS settings (ถ้าใช้ external URL)

## 📊 Storage Quota

Supabase Free Tier:
- **Storage:** 1GB
- **Bandwidth:** 2GB/month

สำหรับ production ควร:
- ใช้ image optimization
- ลบรูปภาพที่ไม่ใช้แล้ว
- ใช้ CDN (ถ้ามี)

## 🔄 Migration จาก URL

ถ้ามีรูปภาพเป็น URL อยู่แล้ว:
1. ไม่ต้องทำอะไร - ระบบรองรับ URL
2. หรืออัพโหลดใหม่ผ่าน Edit Asset Modal

## 💡 Tips

1. **ใช้รูปภาพจาก Supabase Storage** สำหรับรูปภาพที่อัพโหลดเอง
2. **ใช้ URL** สำหรับรูปภาพจาก external sources (Unsplash, etc.)
3. **Backup รูปภาพ** เป็นระยะ (export จาก Supabase)
4. **Monitor Storage usage** ใน Supabase Dashboard

---

**อัพเดทล่าสุด:** 24 ธันวาคม 2568

