# 📋 รูปแบบรหัสทรัพย์สิน - Asset Code Format

## 🎯 รูปแบบมาตรฐาน

**รูปแบบ:** `{PREFIX}{RUNNING}-{DD}-{MM}-{YYYY}`

**ตัวอย่าง:** `A004-09-04-2557`

### ส่วนประกอบ:
- **PREFIX**: อักษรนำหน้าหมวดหมู่ (A-Z)
  - A = คอมพิวเตอร์
  - B = Printer
  - C = กล้องถ่ายรูป
  - ... (ดูในตาราง categories)
- **RUNNING**: เลขลำดับ (3-4 หลัก)
  - 004, 010, 018, ...
- **DD**: วัน (2 หลัก)
  - 09, 18, 31, ...
- **MM**: เดือน (2 หลัก)
  - 04, 08, 12, ...
- **YYYY**: ปี พ.ศ. (4 หลัก)
  - 2557, 2559, 2561, ...

---

## 📝 ตัวอย่างรหัสทรัพย์สิน

| รหัส | ความหมาย |
|------|----------|
| `A004-09-04-2557` | คอมพิวเตอร์ลำดับที่ 4, ซื้อวันที่ 9 เมษายน 2557 |
| `A010-09-04-2557` | คอมพิวเตอร์ลำดับที่ 10, ซื้อวันที่ 9 เมษายน 2557 |
| `A018-03-03-2559` | คอมพิวเตอร์ลำดับที่ 18, ซื้อวันที่ 3 มีนาคม 2559 |
| `B001-15-06-2560` | Printer ลำดับที่ 1, ซื้อวันที่ 15 มิถุนายน 2560 |

---

## 🔧 การใช้งานในโค้ด

### 1. สร้างรหัสทรัพย์สินใหม่

```javascript
import { generateAssetCodeStandard } from '../utils/assetManager';

// สร้างรหัสใหม่
const code = generateAssetCodeStandard(
    'A',           // prefix
    4,             // running number
    new Date('2014-04-09'),  // purchase date
    3              // padding (default: 3)
);
// ผลลัพธ์: 'A004-09-04-2557'
```

### 2. แปลงรหัสเป็นข้อมูล

```javascript
import { parseAssetCode } from '../utils/assetManager';

// แปลงรหัส
const parsed = parseAssetCode('A004-09-04-2557');
// ผลลัพธ์: {
//   prefix: 'A',
//   running: 4,
//   day: 9,
//   month: 4,
//   yearBE: 2557,
//   yearAD: 2014,
//   purchaseDate: Date object
// }
```

### 3. ดึงวันที่ซื้อจากรหัส

```javascript
import { parseAssetCode } from '../utils/assetManager';

const code = 'A004-09-04-2557';
const parsed = parseAssetCode(code);
if (parsed) {
    const purchaseDate = parsed.purchaseDate;
    // purchaseDate = Date object: 2014-04-09
}
```

---

## 📊 ข้อมูลใน Database

### ตาราง `assets`
- **code**: VARCHAR(50) NOT NULL UNIQUE
- รูปแบบ: `{PREFIX}{RUNNING}-{DD}-{MM}-{YYYY}`
- ตัวอย่าง: `A004-09-04-2557`

### ตาราง `categories`
- **prefix**: VARCHAR(10) NOT NULL UNIQUE
- อักษรนำหน้าหมวดหมู่ (A-Z)

---

## 🔄 Migration และ Import

### จากไฟล์ Excel
- รหัสทรัพย์สินถูกอ่านจากคอลัมน์ C (เลขทะเบียน)
- รูปแบบ: `A004-09-04-2557`
- วันที่ซื้อจะถูกดึงจากรหัสอัตโนมัติ (ถ้าไม่มีในคอลัมน์ D)

### Function ใน Database
- `extract_date_from_code(asset_code)` - ดึงวันที่จากรหัส
- รองรับรูปแบบ: `{PREFIX}{RUNNING}-{DD}-{MM}-{YYYY}`

---

## ⚙️ การตั้งค่าในระบบ

### Settings > รหัสทรัพย์สิน
- **รูปแบบ (Pattern)**: `{PREFIX}{RUNNING}-{DD}-{MM}-{YYYY}`
- **จำนวนหลักเลขรัน (Padding)**: 3
- **ตัวอย่าง**: `A004-09-04-2557`

---

## 📌 หมายเหตุ

1. **ปี พ.ศ. vs ค.ศ.**
   - รหัสใช้ปี พ.ศ. (เช่น 2557)
   - Database เก็บเป็น ค.ศ. (เช่น 2014)
   - การแปลง: ค.ศ. = พ.ศ. - 543

2. **รหัสซ้ำ**
   - ระบบจะตรวจสอบรหัสซ้ำอัตโนมัติ
   - ใช้ `ON CONFLICT DO UPDATE` เมื่อ import

3. **รูปแบบเก่า (รองรับ)**
   - รูปแบบเก่า: `{PREFIX}-{YEAR}-{RUNNING}` (เช่น COM-2567-0001)
   - ยังรองรับอยู่ แต่แนะนำให้ใช้รูปแบบใหม่

---

**อัพเดทล่าสุด:** 24 ธันวาคม 2568  
**เวอร์ชัน:** 1.0

