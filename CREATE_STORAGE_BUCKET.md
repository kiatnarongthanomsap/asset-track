# 🪣 คู่มือการสร้าง Storage Bucket ใน Supabase

## ⚠️ Error: "Bucket 'asset-images' not found"

หากคุณเห็น error นี้ หมายความว่ายังไม่ได้สร้าง Storage bucket ใน Supabase

## 🚀 วิธีสร้าง Bucket (แนะนำ - ใช้ Dashboard)

### ขั้นตอนที่ 1: เปิด Supabase Dashboard

1. ไปที่ [https://app.supabase.com](https://app.supabase.com)
2. เลือกโปรเจกต์ของคุณ
3. คลิก **Storage** ในเมนูด้านซ้าย

### ขั้นตอนที่ 2: สร้าง Bucket ใหม่

1. คลิกปุ่ม **New bucket** (หรือ **Create bucket**)
2. กรอกข้อมูล:
   - **Name:** `asset-images` (ต้องตรงกับชื่อนี้)
   - **Public bucket:** ✅ เลือก (ถ้าต้องการให้เข้าถึงได้โดยไม่ต้อง login)
     - หรือ ❌ ไม่เลือก (ถ้าต้องการความปลอดภัยสูง - ต้องตั้งค่า policies)
3. คลิก **Create bucket**

### ขั้นตอนที่ 3: ตั้งค่า Policies (ถ้าใช้ Private bucket)

ถ้าคุณเลือก **Private bucket** ต้องตั้งค่า policies:

1. ไปที่ **Storage** → **Policies**
2. เลือก bucket `asset-images`
3. คลิก **New Policy**
4. สร้าง policies ตามนี้:

#### Policy 1: Public Read Access
```sql
-- Allow public to read files
CREATE POLICY "Public Access"
ON storage.objects FOR SELECT
USING (bucket_id = 'asset-images');
```

#### Policy 2: Authenticated Upload
```sql
-- Allow authenticated users to upload
CREATE POLICY "Authenticated users can upload"
ON storage.objects FOR INSERT
WITH CHECK (
  bucket_id = 'asset-images' 
  AND auth.role() = 'authenticated'
);
```

#### Policy 3: Authenticated Update
```sql
-- Allow authenticated users to update
CREATE POLICY "Users can update own files"
ON storage.objects FOR UPDATE
USING (
  bucket_id = 'asset-images' 
  AND auth.role() = 'authenticated'
);
```

#### Policy 4: Authenticated Delete
```sql
-- Allow authenticated users to delete
CREATE POLICY "Users can delete own files"
ON storage.objects FOR DELETE
USING (
  bucket_id = 'asset-images' 
  AND auth.role() = 'authenticated'
);
```

## 🔧 วิธีสร้าง Bucket ด้วย SQL (Alternative)

ถ้าต้องการสร้างผ่าน SQL Editor:

```sql
-- สร้าง bucket (ต้องใช้ Management API หรือ Dashboard)
-- Supabase ไม่รองรับการสร้าง bucket ผ่าน SQL โดยตรง
-- ต้องใช้ Dashboard หรือ Management API
```

**หมายเหตุ:** Supabase ไม่รองรับการสร้าง bucket ผ่าน SQL โดยตรง ต้องใช้ Dashboard หรือ Management API

## ✅ ตรวจสอบว่า Bucket ถูกสร้างแล้ว

### วิธีที่ 1: ตรวจสอบใน Dashboard
1. ไปที่ **Storage** → **Buckets**
2. ดูว่ามี bucket ชื่อ `asset-images` หรือไม่

### วิธีที่ 2: ตรวจสอบด้วย SQL
```sql
SELECT name, public, created_at 
FROM storage.buckets 
WHERE name = 'asset-images';
```

## 🎯 แนะนำ: ใช้ Public Bucket

สำหรับ development และการใช้งานทั่วไป แนะนำให้ใช้ **Public bucket** เพราะ:
- ✅ ไม่ต้องตั้งค่า policies ซับซ้อน
- ✅ เข้าถึงได้ง่าย
- ✅ เหมาะสำหรับรูปภาพครุภัณฑ์ทั่วไป

**ข้อควรระวัง:** Public bucket หมายความว่าใครก็สามารถเข้าถึง URL ของรูปภาพได้ แต่ไม่สามารถอัพโหลด/ลบได้ (ถ้าไม่ตั้งค่า policies)

## 🔒 Security สำหรับ Production

สำหรับ production ควร:
1. ใช้ **Private bucket**
2. ตั้งค่า RLS policies ที่เหมาะสม
3. ใช้ service role key สำหรับ operations ที่ต้องการสิทธิ์สูง
4. ตั้งค่า CORS ถ้าจำเป็น

## 🐛 Troubleshooting

### ปัญหา: "Bucket already exists"
**วิธีแก้:** Bucket ถูกสร้างแล้ว ไม่ต้องสร้างใหม่

### ปัญหา: "Permission denied"
**วิธีแก้:** 
- ตรวจสอบว่าใช้ anon key หรือ service role key
- ตรวจสอบ policies ของ bucket

### ปัญหา: "Cannot upload file"
**วิธีแก้:**
- ตรวจสอบ policies สำหรับ INSERT
- ตรวจสอบขนาดไฟล์ (ต้องไม่เกิน 10MB)
- ตรวจสอบประเภทไฟล์ (ต้องเป็นรูปภาพ)

## 📝 Quick Start Checklist

- [ ] เปิด Supabase Dashboard
- [ ] ไปที่ Storage
- [ ] สร้าง bucket ชื่อ `asset-images`
- [ ] เลือก Public bucket (หรือ Private + ตั้งค่า policies)
- [ ] คลิก Create bucket
- [ ] ทดสอบอัพโหลดรูปภาพในแอป

---

**อัพเดทล่าสุด:** 24 ธันวาคม 2568

